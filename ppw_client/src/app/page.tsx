'use client';
import Link from 'next/link';
import { useIssues } from '@/components/issues/issue-provider';
const metrics = [{ label: 'Total de Issues', test: () => true }, { label: 'Backlog', test: (status: string) => status === 'BACKLOG' }, { label: 'Em desenvolvimento', test: (status: string) => status === 'EM_DESENVOLVIMENTO' }, { label: 'Em QA', test: (status: string) => status === 'QA' }, { label: 'Concluídas', test: (status: string) => status === 'CONCLUIDO' }, { label: 'Críticas', test: (_: string, priority: string) => priority === 'CRITICA' }];
export default function DashboardPage() { const { issues, loading, error } = useIssues(); if (loading) return <p>Carregando...</p>; return <><div className="page-head"><div><h1>Dashboard</h1><p className="muted">Visão geral do fluxo de trabalho.</p></div><Link className="button" href="/issues/nova">Nova Issue</Link></div>{error && <p className="error">{error}</p>}<section className="grid">{metrics.map((metric) => <div className="card" key={metric.label}><span className="muted">{metric.label}</span><p className="metric">{issues.filter((issue) => metric.test(issue.status, issue.priority)).length}</p></div>)}</section><section className="card" style={{ marginTop: 20 }}><h2>Distribuição por status</h2>{['BACKLOG', 'TRIAGEM', 'EM_DESENVOLVIMENTO', 'CODE_REVIEW', 'QA', 'PRONTO_PARA_DEPLOY', 'CONCLUIDO'].map((status) => <p key={status}><strong>{status}</strong>: {issues.filter((issue) => issue.status === status).length}</p>)}</section></>; }
