# Frontend Bug Tracker

Execute `npm install` e `npm run dev`. A aplicação está em `http://localhost:3000` e chama a API em `http://localhost:8000` por meio de `src/lib/api.ts`.

`src/lib/storage.ts` centraliza o localStorage, que é a persistência permanente do usuário.
