# Backend Bug Tracker

API NestJS em `http://localhost:8000`. Execute `npm install` e `npm run start:dev`.

As rotas são `GET/POST/PATCH/DELETE /issues` e `GET /projetos`. Os dados do service são mantidos apenas em memória; não há banco de dados.
